@{
    Layout = "_Layout";
}

@using AnimalShelter.Models;

@if (@Model.Count == 0)
{
  <h3>No animals have been found.</h3>
} 

@foreach (Animal animal in Model)
{
  <li>@Html.ActionLink($"{animal.Type}", "Details", new { id = animal.AnimalId })</li>
}

@Html.ActionLink("Back to Search", "Index")